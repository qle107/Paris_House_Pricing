# Platform / application dependencies (conflict-resolved, June 2026).
# NOTE: Apache Airflow is intentionally NOT here -- it pins SQLAlchemy<2.0 and
# many other libs, which conflicts with the app stack. Airflow runs in its own
# Docker image (docker/docker-compose.yml). For a local scheduler install, use
# requirements-airflow.txt with Airflow's official constraints file.

# Core data stack
pandas==2.2.2
polars==1.6.0
duckdb==1.1.0
numpy==1.26.4
pyarrow==17.0.0

# Database
SQLAlchemy==2.0.34
psycopg2-binary==2.9.9
geoalchemy2==0.15.2
pgvector==0.3.3

# GIS
geopandas==1.0.1
shapely==2.0.6
pyproj==3.6.1
rasterio==1.3.10
fiona==1.10.0
rtree==1.3.0

# HTTP / scraping
requests==2.32.3
tenacity==8.5.0          # <9 required by streamlit; 8.5.0 keeps modern retry API
beautifulsoup4==4.12.3
lxml==5.3.0
playwright==1.47.0
# selenium==4.24.0       # only if a target requires it

# Graph / ML
networkx==3.3
scikit-learn==1.5.1
xgboost==2.1.1
lightgbm==4.5.0
joblib==1.4.2
shap==0.46.0            # per-area model attributions (optional; degrades to gain importances)

# AI / RAG (manual mode needs embeddings only)
ollama==0.3.3                       # free local LLM client (optional auto mode)
sentence-transformers==3.1.1        # free local embeddings (multilingual-e5)
pypdf==5.0.0
pdfplumber==0.11.4
openai==1.51.0                      # optional paid drop-in (lazy-imported)

# Config
pydantic==2.9.2
pydantic-settings==2.5.2
pyyaml==6.0.2
python-dotenv==1.0.1

# Validation / tests
pandera==0.20.4
pytest==8.3.3
pytest-mock==3.14.0

# Dashboard
streamlit==1.38.0
plotly==5.24.1

# Map API
fastapi==0.115.0
uvicorn[standard]==0.30.6
